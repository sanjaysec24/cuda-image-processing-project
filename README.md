# CUDA Image Processing Project

This project demonstrates GPU accelerated computation using NVIDIA CUDA.

The program uses a CUDA kernel to process data in parallel on the GPU.

Features
- CUDA kernel execution
- GPU memory allocation
- Host to device memory transfer
- Parallel processing

Compilation:
nvcc main.cu -o program

Execution:
./program

Output:
GPU computation completed successfully
